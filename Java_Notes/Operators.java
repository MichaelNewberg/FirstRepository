public class Operators {
    public static void main(String[] args) {
        // (+) addition
        // (-) subtraction
        // (*) multiplication
        // (/) division
        // (%) modulus/remainder
        // (=) assignment

        //QoL operators
        // (+=) addition of the variable to itself     x+=5;  is the same as x=x+5;
        // (-=) subtraction of the variable to itself  x-=5;
        // (++) increment operator adds 1              x++  OR  ++x
        // (--) decrement operator subtracts 1         x--  OR  --x

        //Logical operators
        // (<, >) Less than/Greater than
        // (==) equivalence
        // (===) strict equivalence
        // (!) not operator (inverses requirement)
        // (&&) logical AND
        // (||) logical OR
    }
}
